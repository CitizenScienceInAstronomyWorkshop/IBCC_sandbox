testDataFile = sprintf('%s/data/%s_test_data.mat', ...
    expSettings.topSaveDir, ...
    expSettings.expLabel);

trainingDataFile = sprintf('%s/data/%s_training_data.mat', ...
    expSettings.topSaveDir, ...
    expSettings.expLabel);

agentsFile = sprintf('%s/data/%s_agents.mat', ...
        expSettings.topSaveDir, expSettings.expLabel);   

delete(testDataFile, trainingDataFile, agentsFile);