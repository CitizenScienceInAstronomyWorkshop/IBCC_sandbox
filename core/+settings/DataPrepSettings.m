classdef DataPrepSettings
    %DATAPREPSETTINGS Settings for preparing and preprocessing real
    %datasets
    %   Used for filtering and subsampling datasets, and switching between
    %   sparse "scoreset" format and full matrix formats for data. Also
    %   used to store locations of files containing data/results.
    
    properties

    end
    
    methods
        function obj = DataPrepSettings()
        end
        
    end
    
end

